export default function Topbar() {
  return (
    <header
      style={{
        height: 60,
        borderBottom: "1px solid #eee",
        display: "flex",
        alignItems: "center",
        padding: "0 20px",
      }}
    >
      <span>Dashboard</span>
    </header>
  );
}
